{% set classes = ['fa-' ~ family, 'fa-' ~ style, 'fa-' ~ name, 'fa-' ~ size] %}
{% if fixedWidth %}
    {% set classes = classes|merge(['fa-fw']) %}
{% endif %}
<i {{ attributes.defaults({
    class: classes|join(' '),
}) }}></i>
